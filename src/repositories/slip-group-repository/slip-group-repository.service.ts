import { Injectable } from '@nestjs/common';

@Injectable()
export class SlipGroupRepositoryService {
  // constructor(
  //   @InjectRepository(SlipGroupEntity)
  //   private readonly slipUserRepository: Repository<SlipGroupEntity>,
  // ) {
  // }

  // async create(SlipGroupEntity: SlipGroupEntity): Promise<SlipGroupEntity> {
  //   return await this.slipUserRepository.save(SlipGroupEntity);
  // }

  // async findAll(
  //   options?: FindManyOptions<SlipGroupEntity>,
  // ): Promise<SlipGroupEntity[]> {
  //   return await this.slipUserRepository.find(options);
  // }

  // async count(options?: FindManyOptions<SlipGroupEntity>): Promise<number> {
  //   return await this.slipUserRepository.count(options);
  // }

  // async findOne(options: FindOneOptions<SlipGroupEntity>) {
  //   return await this.slipUserRepository.findOne(options);
  // }

  // async update(id: string, body: SlipGroupEntity) {
  //   return await this.slipUserRepository.update(id, body);
  // }

  // async delete(id: string) {
  //   return await this.slipUserRepository.delete(id);
  // }

  // async findAndCount(
  //   options?: FindManyOptions<SlipGroupEntity>,
  // ): Promise<[SlipGroupEntity[], number]> {
  //   return await this.slipUserRepository.findAndCount(options);
  // }

  // createQueryBuilder(
  //   alias: string,
  //   queryRunner?: QueryRunner,
  // ): SelectQueryBuilder<SlipGroupEntity> {
  //   return this.slipUserRepository.createQueryBuilder(alias);
  // }
}
